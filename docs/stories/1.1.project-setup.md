# Story 1.1: Project Setup & Environment

## Status
InReview

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "eslint", "tsc"]

## Story
**As a** desenvolvedor,
**I want** um projeto Next.js 15 configurado com TypeScript, Tailwind CSS, shadcn/ui e variáveis de ambiente,
**so that** a base técnica esteja pronta para implementar todas as features do Prospect Clinic sem retrabalho de setup.

## Acceptance Criteria
1. Projeto Next.js 15+ criado com TypeScript, Tailwind CSS e shadcn/ui instalados e funcionando
2. Arquivo `.env.local` criado com as variáveis `APIFY_TOKEN` e `GEMINI_API_KEY` (valores placeholder)
3. Arquivo `.env.example` versionado no git com os nomes das variáveis (sem valores reais)
4. Rota de health check `GET /api/health` retorna `{ status: "ok", timestamp: "..." }`
5. `npm run dev` inicia o servidor sem erros em `localhost:3000`

## 🤖 CodeRabbit Integration

**Story Type Analysis**
- **Primary Type**: Architecture (setup/infraestrutura)
- **Secondary Type(s)**: Deployment (configuração de ambiente)
- **Complexity**: Low — escopo limitado a configuração inicial

**Specialized Agent Assignment**
- **Primary Agents**: @dev
- **Supporting Agents**: @architect (validação de estrutura de pastas)

**Quality Gate Tasks**
- [ ] Pre-Commit (@dev): Verificar estrutura de pastas, tsconfig, tailwind.config e components.json antes de marcar completo

**Self-Healing Configuration**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL

**CodeRabbit Focus Areas**
- **Primary Focus**:
  - Credenciais NUNCA hardcoded — apenas em `.env.local` (gitignored)
  - `.env.example` sem valores reais, apenas nomes das variáveis
- **Secondary Focus**:
  - `tsconfig.json` com `paths` configurado para alias `@/*`
  - `components.json` do shadcn/ui corretamente configurado

## Tasks / Subtasks

- [x] Task 1 — Criar projeto Next.js (AC: 1)
  - [x] Executar `npx create-next-app@latest prospect-clinic --typescript --tailwind --app --src-dir --import-alias "@/*"`
  - [x] Verificar que `npm run dev` sobe sem erros

- [x] Task 2 — Instalar dependências (AC: 1)
  - [x] `npm install apify-client @google/generative-ai uuid`
  - [x] `npm install -D @types/uuid`
  - [x] Inicializar shadcn/ui: `npx shadcn@latest init`
  - [x] Instalar componentes: `npx shadcn@latest add table badge select sheet button input dialog`

- [x] Task 3 — Configurar variáveis de ambiente (AC: 2, 3)
  - [x] Criar `.env.local` com `APIFY_TOKEN=` e `GEMINI_API_KEY=` e `GEMINI_TIMEOUT_MS=10000`
  - [x] Criar `.env.example` com os mesmos nomes (sem valores)
  - [x] Confirmar que `.env.local` está no `.gitignore`

- [x] Task 4 — Criar rota health check (AC: 4)
  - [x] Criar `src/app/api/health/route.ts` com GET retornando `{ status: "ok", timestamp: new Date().toISOString() }`

- [x] Task 5 — Criar estrutura de pastas (AC: 1, 5)
  - [x] Criar `src/components/leads/`, `src/components/layout/`, `src/hooks/`, `src/lib/`, `src/types/`
  - [x] Criar `data/` com `.gitkeep` (pasta para leads.json, gitignored)
  - [x] Criar `src/types/lead.ts`, `src/types/scrape.ts`, `src/types/messages.ts` com interfaces vazias

- [x] Task 6 — Validação final (AC: 5)
  - [x] `npm run dev` sem erros
  - [x] `npm run lint` sem erros
  - [x] `npm run build` compila sem erros TypeScript

## Dev Notes

### Stack e Versões
[Source: docs/architecture.md#tech-stack]
- **Runtime**: Node.js v22 (já instalado)
- **Framework**: Next.js 15+ com App Router + TypeScript 5.x
- **Styling**: Tailwind CSS 3.x + shadcn/ui
- **SDK Apify**: `apify-client` (npm)
- **SDK Gemini**: `@google/generative-ai` (npm)
- **UUID**: `uuid` v9.x

### Estrutura de Pastas Esperada
[Source: docs/architecture.md#unified-project-structure]
```
src/
├── app/
│   ├── page.tsx
│   ├── layout.tsx
│   ├── globals.css
│   ├── settings/page.tsx
│   └── api/
│       ├── health/route.ts       ← criar nesta story
│       ├── leads/...
│       ├── scrape/...
│       └── settings/...
├── components/
│   ├── leads/
│   ├── layout/
│   └── ui/                      ← gerado pelo shadcn/ui CLI
├── hooks/
├── lib/
└── types/
    ├── lead.ts                  ← criar esqueleto nesta story
    ├── scrape.ts
    └── messages.ts
data/                            ← gitignored, armazena leads.json
```

### TypeScript Interfaces a criar (esqueleto)
[Source: docs/architecture.md#data-models]
```typescript
// src/types/lead.ts
export type LeadStatus = "novo" | "contatado" | "respondeu" | "fechado" | "descartado";
export type Platform = "instagram" | "linkedin";

export interface Lead {
  id: string;
  name: string;
  profileUrl: string;
  platform: Platform;
  bio: string;
  followersCount: number;
  status: LeadStatus;
  message: string | null;
  createdAt: string;
}

export interface LeadsStore {
  leads: Lead[];
  updatedAt: string;
}
```

### Variáveis de Ambiente
[Source: docs/architecture.md#environment-configuration]
```bash
# .env.local (NUNCA versionar)
APIFY_TOKEN=
GEMINI_API_KEY=
GEMINI_TIMEOUT_MS=10000
```

### Regras Críticas
[Source: docs/architecture.md#coding-standards]
- Credenciais **exclusivamente** em `.env.local` (Next.js server-only)
- `.env.local` e `data/` no `.gitignore`
- Alias `@/*` → `src/*` configurado no tsconfig

### Testing
[Source: docs/architecture.md#testing-strategy]
- Ferramenta: **Vitest**
- Nesta story não há lógica a testar — apenas configuração
- Verificar que `npm run build` compila sem erros TypeScript (substitui testes nesta story)

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-04-02 | 3.0 | Story validada e aprovada pelo @po - Status: Ready | Pax |
| 2026-04-02 | 2.0 | Story implementada com sucesso - todos os tasks completados | Dex |
| 2026-04-01 | 1.0 | Story criada | River |

## Dev Agent Record
_(Preenchido pelo @dev durante implementação)_

### Agent Model Used
Claude Sonnet 4.6 (claude-sonnet-4-6)

### Debug Log References
- ESLint configuration issue: Framework files (.aiox-core, .claude) were being linted. Fixed by updating eslint.config.mjs to add global ignores for .aiox-core, .aiox, and .claude directories.
- Deleted .eslintignore file (deprecated in ESLint 9+) in favor of eslint.config.mjs configuration.

### Completion Notes
All 6 tasks completed successfully:
- Next.js 15 project initialized with TypeScript, Tailwind CSS, App Router, and shadcn/ui
- All dependencies installed (apify-client, @google/generative-ai, uuid, @types/uuid, shadcn/ui components)
- Environment files created: .env.local (gitignored) and .env.example (versioned)
- Health check API endpoint implemented at GET /api/health
- Project directory structure created with all required folders (components, hooks, lib, types)
- TypeScript type definitions created for Lead, Platform, LeadStatus, ScrapeRequest, ScrapeResult, BatchGenerateRequest, BatchGenerateResult
- All validation commands pass: dev server, eslint, build/typescript compilation

### File List

**New Files Created:**
- `.env.local` — Environment variables with placeholders (gitignored)
- `.env.example` — Template for environment variables (versioned)
- `eslint.config.mjs` — Updated to ignore framework directories
- `src/app/api/health/route.ts` — Health check endpoint returning status and timestamp
- `src/types/lead.ts` — Core Lead interface and related types (LeadStatus, Platform, LeadsStore)
- `src/types/scrape.ts` — Apify scraping request/response interfaces
- `src/types/messages.ts` — Gemini message generation request/response interfaces
- `data/.gitkeep` — Placeholder for local leads.json storage directory

**Directories Created:**
- `src/components/leads/` — Components for lead management UI
- `src/components/layout/` — Layout components (header, footer, sidebar)
- `src/hooks/` — Custom React hooks (useLeads, useGenerateMessage, etc.)
- `src/lib/` — Utility services (leadsRepository, apifyService, geminiService, csvExporter)
- `src/types/` — TypeScript type definitions
- `data/` — Local storage directory (gitignored in production)

**Modified Files:**
- `.gitignore` — Already included .env.local and data/ (no changes needed)
- `eslint.config.mjs` — Added global ignores for .aiox-core, .aiox, .claude directories

**shadcn/ui Components Installed:**
- `button` — Form and action buttons
- `input` — Text input fields
- `table` — Lead list display
- `badge` — Status indicators
- `select` — Filter dropdowns
- `sheet` — Lead detail panel/drawer
- `dialog` — Modal dialogs for new search

## QA Results
_(Preenchido pelo @qa após revisão)_
