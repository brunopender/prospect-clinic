# Story 3.3: Lead Status Management & Export

## Status
InReview

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "eslint", "tsc"]

## Story
**As a** usuário,
**I want** atualizar o status de cada lead e exportar minha lista para CSV,
**so that** eu possa acompanhar meu pipeline de prospecção e compartilhar dados.

## Acceptance Criteria
1. Dropdown de status (Novo, Contatado, Respondeu, Fechado, Descartado) atualiza via `PATCH /api/leads/[id]`
2. Mudança de status persiste no JSON local imediatamente
3. Botão "Exportar CSV" baixa arquivo com todos os leads visíveis (respeitando filtros ativos)
4. CSV inclui colunas: nome, plataforma, profileUrl, status, mensagem, data de captura
5. Nenhuma dependência com a Story 3.2 (status pode ser atualizado diretamente na tabela)

## 🤖 CodeRabbit Integration

**Story Type Analysis**
- **Primary Type**: API (update operation + CSV generation)
- **Secondary Type(s)**: Frontend (dropdown + export button)
- **Complexity**: Low — CRUD simples + string formatting

**Specialized Agent Assignment**
- **Primary Agents**: @dev
- **Supporting Agents**: N/A

**Quality Gate Tasks**
- [ ] Pre-Commit (@dev): Verificar que PATCH persiste corretamente; CSV encoding UTF-8; filtros respeitados na exportação

**CodeRabbit Focus Areas**
- **Primary Focus**:
  - Endpoint PATCH /api/leads/[id] valida status enum
  - CSV exporta corretamente com delimiter `,` e encoding UTF-8
- **Secondary Focus**:
  - Download do arquivo com nome correto (`leads-YYYY-MM-DD.csv`)

## Tasks / Subtasks

- [ ] Task 1 — Criar Route Handler PATCH /api/leads/[id] (AC: 1, 2)
  - [ ] Criar `src/app/api/leads/[id]/route.ts` com método PATCH
  - [ ] Validar body: `status` deve ser um dos values enum de `LeadStatus`
  - [ ] Chamar `leadsRepository.update(id, { status })`
  - [ ] Retornar lead atualizado com status 200
  - [ ] Retornar 404 se lead não encontrado

- [ ] Task 2 — Criar componente SelectStatus na tabela (AC: 1)
  - [ ] Atualizar `LeadsTable` para incluir dropdown de status em cada linha
  - [ ] Usar `Select` do shadcn/ui
  - [ ] Ao mudar, fazer PATCH para atualizar
  - [ ] Mostrar loading state durante update

- [ ] Task 3 — Criar função exportar CSV (AC: 3, 4)
  - [ ] Criar `src/lib/csv-exporter.ts`
  - [ ] Função: `leadsToCSV(leads: Lead[]): string`
  - [ ] Headers: "Nome,Plataforma,URL,Status,Mensagem,Data de Captura"
  - [ ] Escapar valores com aspas duplas se contiverem vírgulas ou quebras de linha
  - [ ] Retornar string CSV

- [ ] Task 4 — Criar Route Handler GET /api/leads/export (AC: 3, 4)
  - [ ] Criar `src/app/api/leads/export/route.ts`
  - [ ] Aceitar query params `platform` e `status` para filtrar
  - [ ] Buscar leads via `leadsRepository.getAll(filters)`
  - [ ] Converter para CSV via `leadsToCSV()`
  - [ ] Retornar como arquivo com headers: `Content-Type: text/csv`, `Content-Disposition: attachment; filename="leads-...csv"`

- [ ] Task 5 — Criar botão de exportação (AC: 3, 4)
  - [ ] Adicionar botão "Exportar CSV" na página principal
  - [ ] Ao clicar: fazer GET para `/api/leads/export?platform=...&status=...` respeitando filtros ativos
  - [ ] Navegador baixa arquivo automaticamente

- [ ] Task 6 — Testes unitários (AC: 1, 3, 4)
  - [ ] Testar PATCH com status válido e inválido
  - [ ] Testar CSV generation com caracteres especiais
  - [ ] Testar que filtros são respeitados na exportação

## Dev Notes

### PATCH /api/leads/[id]
[Source: docs/architecture.md#api-specification]
```typescript
// src/app/api/leads/[id]/route.ts
import { NextRequest, NextResponse } from "next/server";
import { leadsRepository } from "@/lib/leads-repository";
import type { LeadStatus } from "@/types/lead";

const VALID_STATUSES: LeadStatus[] = ["novo", "contatado", "respondeu", "fechado", "descartado"];

export async function PATCH(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  try {
    const body = await request.json();
    if (!VALID_STATUSES.includes(body.status)) {
      return NextResponse.json({ error: "Status inválido" }, { status: 400 });
    }
    const updated = await leadsRepository.update(params.id, { status: body.status });
    if (!updated) {
      return NextResponse.json({ error: "Lead não encontrado" }, { status: 404 });
    }
    return NextResponse.json(updated);
  } catch (error) {
    console.error("[PATCH /api/leads/[id]]", error);
    return NextResponse.json({ error: "Falha ao atualizar" }, { status: 500 });
  }
}
```

### CSV Exporter
[Source: docs/architecture.md#data-models]
```typescript
// src/lib/csv-exporter.ts
import type { Lead } from "@/types/lead";

function escapeCsvValue(value: string): string {
  if (value.includes(",") || value.includes('"') || value.includes("\n")) {
    return `"${value.replace(/"/g, '""')}"`;
  }
  return value;
}

export function leadsToCSV(leads: Lead[]): string {
  const headers = ["Nome", "Plataforma", "URL", "Status", "Mensagem", "Data"];
  const rows = leads.map(lead => [
    escapeCsvValue(lead.name),
    escapeCsvValue(lead.platform),
    escapeCsvValue(lead.profileUrl),
    escapeCsvValue(lead.status),
    escapeCsvValue(lead.message ?? ""),
    escapeCsvValue(lead.createdAt),
  ]);
  return [headers, ...rows].map(row => row.join(",")).join("\n");
}
```

### GET /api/leads/export
[Source: docs/architecture.md#api-specification]
```typescript
// src/app/api/leads/export/route.ts
import { NextRequest, NextResponse } from "next/server";
import { leadsRepository } from "@/lib/leads-repository";
import { leadsToCSV } from "@/lib/csv-exporter";

export async function GET(request: NextRequest) {
  try {
    const { searchParams } = new URL(request.url);
    const platform = searchParams.get("platform") ?? undefined;
    const status = searchParams.get("status") ?? undefined;

    const leads = await leadsRepository.getAll({ platform, status } as any);
    const csv = leadsToCSV(leads);

    const date = new Date().toISOString().split("T")[0];
    return new NextResponse(csv, {
      headers: {
        "Content-Type": "text/csv; charset=utf-8",
        "Content-Disposition": `attachment; filename="leads-${date}.csv"`,
      },
    });
  } catch (error) {
    console.error("[GET /api/leads/export]", error);
    return NextResponse.json({ error: "Falha na exportação" }, { status: 500 });
  }
}
```

### Status Colors/Badges
[Source: docs/architecture.md#frontend-architecture]
- novo: blue
- contatado: yellow
- respondeu: green
- fechado: gray
- descartado: red

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-04-02 | 2.0 | Story validada e aprovada pelo @po - Status: Ready | Pax |
| 2026-04-01 | 1.0 | Story criada | River |

## Validação PO (2026-04-02)
| 2026-04-01 | 1.0 | Story criada | River |

## Dev Agent Record
_(Preenchido pelo @dev durante implementação)_

### Agent Model Used
### Debug Log References
### Completion Notes
### File List

## QA Results
_(Preenchido pelo @qa após revisão)_
